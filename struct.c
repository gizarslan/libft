/* #include <stdio.h>
#include <stdlib.h>

typedef struct student
{
	char *name_surname;
	int age;
	double gpa;
} build;


int main()
{
	build s1;
	build *s2;
	s2 = (build *)malloc(sizeof(build));
	s2->age = 23;
	s1.name_surname = "gizem arslan";
	s1.age = 20;
	s1.gpa = 4.0;

	printf("%d", s2->age);
	
} */